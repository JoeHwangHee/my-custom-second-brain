# _router.md — 의도 분기 파이프라인

모든 입력은 이 파일의 5단계를 순서대로 거쳐 작업 유형이 결정된다.
의도 확정 후 해당 규칙 파일로 이동한다.

---

## Stage 0 — 현재 시각 확보 + _pending.md 확인 (항상 최우선)

작업 시작 시 현재 시각을 1회 확보한다.

```
shell `date '+%Y-%m-%dT%H:%M:%S'` 1회 실행 → 결과를 이번 작업 전체에서 재사용한다.
용도: log.md timestamp, Thought ID의 날짜부, _lint_status.md 갱신,
      Lint 트리거 조건 B("현재 시각 − last_lint ≥ 24h") 판단.
LLM은 현재 시각을 자체적으로 알 수 없으므로 이 확보 없이 timestamp를 추정하지 않는다.
```

이어서 _pending.md를 확인한다.

```
미결 항목 존재 시:
  → 사용자에게 안내: "이전에 미결된 항목이 있습니다: [내용]"
  → 처리 여부 확인 후 원래 의도로 복귀

미결 항목 없음:
  → Stage 1로 진행
```

---

## Stage 1 — 명시적 명령어 감지 (결정론적)

입력에 아래 키워드가 포함되면 즉시 해당 작업으로 라우팅한다.
이후 단계 스킵.

```
Ingest 확정: "저장해줘", "기록해줘", "추가해줘"
Query 확정:  "찾아줘", "조회해줘", "보여줘", "검색해줘"
Lint 확정:   "정비해줘", "점검해줘", "Lint 실행", "정리해줘"
Delete 확정: "삭제해줘", "지워줘", "삭제"
```

---

## Stage 2 — 문법/패턴 매칭 (준결정론적)

Stage 1 미결 시 문장 구조로 판단한다.
이후 단계 스킵.

```
Ingest 확정 패턴:
  - 1인칭 + 과거형 + 새로운 사실 서술
  - "~했다", "~라는 걸 알았다", "~라고 한다"

Query 확정 패턴:
  - 의문형 + 정보 탐색 의도
  - "뭐가 있어?", "어떻게 ~해?", "~에 대해 알려줘"

Delete 확정 패턴:
  - 대상 + 제거 의도
  - "~ 삭제", "~ 지워", "~ 없애"
```

---

## Stage 3 — LLM 맥락적 의도 파악

Stage 1, 2로 의도 미결 시에만 진입한다.

**역할**: 의도 분류만 수행. 카테고리 결정·내용 판단 금지.

```
처리 항목:
  - 지시 대명사 해소 ("그거 찾아줘" → 직전 주제로 대상 특정)
  - 대화 흐름 파악 (이전 맥락이 Query였으면 후속도 Query 가능성 높음)
  - 암묵적 의도 분류

출력 형식:
  {
    intent_type: Ingest | Query | Lint | Mixed | Other,
    subject: "정규화된 주제 (자연어)",
    context_note: "맥락 요약 한 줄" (선택)
  }
```

Stage 3의 subject 필드는 이후 Query 캐스케이드의 입력으로 사용된다.

---

## Stage 4 — 복합 의도 처리

Stage 3에서 Mixed 판정 시 적용된다.

```
처리 순서: Ingest 먼저 실행 → 완료 후 Query 실행

예시:
  "오늘 독서했는데 관련 내용 있어?"
  → 독서 기록 Ingest → 완료 → 독서 관련 Query
```

---

## Stage 5 — 모호성 해소

여전히 의도 불명확 시 적용된다.

```
지식베이스와 무관한 일반 대화로 판단:
  → 라우팅 없이 일반 응답 반환, log.md에 OTHER 기록

판단 불가:
  → 사용자에게 확인: "저장하시겠습니까, 조회하시겠습니까?"
```

---

## 의도 확정 후 이동

```
Ingest 확정 → _rules/storage_rules.md
               Stage 3 subject를 저장 대상 주제로 사용

Query 확정  → _rules/query_rules.md
               Stage 3 subject를 쿼리 입력으로 사용

Lint 확정   → _rules/lint_rules.md

Delete 확정 → _rules/storage_rules.md 의 "삭제 핸들러"
               Stage 3 subject로 삭제 대상 파일을 특정
```
